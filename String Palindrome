import re
class Solution:
    def isPalindrome(self, s: str) -> bool:
        s = re.sub(r'[^a-zA-Z]','', s.lower())
        x = s[::-1]
        if x == s:
            return True
        else:
            return False
